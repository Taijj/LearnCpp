#include <iostream> // Input/Output for Console

int main() // case sensitive, point of entry
{
    // Shift thing into the output stream <<
    std::cout << "Hello World!" << std::endl; // <- endl alt for \n

    return 0; // Return code 0 is "success", otherwise "error"
}
