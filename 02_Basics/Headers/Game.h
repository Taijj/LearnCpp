
// C++ way for "Include Guards"
// Only include this once, if multiple nested including is happening
#pragma once

// C way for "Include Guards"
//#ifndef UNIQUE_NAME
//#define UNIQUE_NAME

// They are like interfaces
void DoFoo();
void DoBar();
void DoElse(int param);

//#endif // End of C include guard
